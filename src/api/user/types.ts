// No payload is required to start the flow
export type GoogleStart = void;

/* Backend does not expose these yet – uncomment when ready
export interface MeRes {
  user: {
    _id: string;
    email: string;
    name?: string;
    picture?: string;
  };
}

export interface LogoutRes {
  message: string;
}
*/